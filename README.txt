this is a guide for how to make projects with this tool.
how works?
this engine uses raycasting, have more than 1 layer, 1.0 have 2, 1.1 have 3.
how use layers?
the engine contqin diferent objects for layers, they are named "lay(num)" . Now, you need to change the form to the map of you game.
how to change aspect?
all layers color is signed on "raycaster" object. The custom block named "ScreenLine" contain all the info about the layers (Y pos, Color).
That is all the info you need know for now.
